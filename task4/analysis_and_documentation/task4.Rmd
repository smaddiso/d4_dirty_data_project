---
title: "R Notebook"
output: html_notebook
---

** load libraries **

```{r}
library(tidyverse)
library(dplyr)
library(janitor)
library(here)
library(readr)
library(readxl)
```

** read cleaned data file **

```{r}
candy <- read_csv("../clean_data/boing_boing_candy_clean.csv")
```

** review data **

```{r}
dim(candy)
```

```{r}
glimpse(candy)
```
